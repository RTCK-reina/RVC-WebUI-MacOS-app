[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -v
    --tb=short
    --strict-markers
markers =
    gpu: GPU (MPS/CUDA) が必要なテスト
    slow: 実行時間が長いテスト
    integration: 結合テスト（外部プロセス起動など）
